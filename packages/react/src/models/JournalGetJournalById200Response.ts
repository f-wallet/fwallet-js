/* tslint:disable */
/* eslint-disable */
/**
 * FWallet API
 * # FWallet API  Multi-tenant financial wallet infrastructure for emerging markets.  **Base URL:** `https://api.fwallet.co.ug`  ## Quick Start for Developers  1. **Create an organization** — `POST /v1/tenants` with a name, slug, and currency config 2. **Get an API key** — `POST /v1/tenants/{id}/api-keys` (save the key — it\'s shown only once) 3. **Set the header** — Add `X-API-Key: {your-key}` to all subsequent requests 4. **Create wallets** — `POST /v1/wallets` for each user/branch 5. **Fund wallets** — `POST /v1/deposits/momo-webhook` to simulate a MoMo deposit 6. **Transfer money** — `POST /v1/transfers` with an `Idempotency-Key` header 7. **Request payouts** — `POST /v1/payouts` (requires a different person to approve)  ## Key Concepts  - **Multi-tenancy**: Every API call is scoped to a tenant (organization) via the API key. Tenants are fully isolated. - **Double-entry ledger**: Every money movement is recorded as a journal entry with balanced debit/credit lines. Entries are immutable — corrections are posted as reversing entries. - **Money as bigint**: All amounts are stored in the smallest currency unit (e.g. 1 UGX = 1, 1 USD = 100 cents). API responses return these as strings. - **Idempotency**: All POST endpoints that create financial records require an `Idempotency-Key` header. Replaying the same key returns the original result. - **Maker-checker**: Payouts require approval from a person different from the requestor.  ## Error Codes  | Code | Status | Description | |------|--------|-------------| | `INSUFFICIENT_BALANCE` | 422 | Wallet doesn\'t have enough funds (amount + fee) | | `CURRENCY_MISMATCH` | 422 | Transfer between wallets of different currencies | | `WALLET_NOT_FOUND` | 404 | Wallet ID doesn\'t exist or belongs to another tenant | | `WALLET_FROZEN` | 422 | Wallet is frozen — can receive but not send | | `INVALID_API_KEY` | 401 | Missing, invalid, or expired API key | | `RISK_CHECK_FAILED` | 422 | Transaction exceeds per-transaction or daily limit | | `MAKER_CHECKER_VIOLATION` | 422 | Cannot approve your own payout request | | `UNBALANCED_ENTRY` | 422 | Journal entry debits don\'t equal credits (internal error) | | `IDEMPOTENCY_CONFLICT` | 409 | Same idempotency key with different request body |  ## Authentication  Tenant-scoped endpoints accept either bearer API keys via `X-API-Key` or HMAC signed requests via `X-FWallet-Key-Id`, `X-FWallet-Timestamp`, `X-FWallet-Nonce`, `X-FWallet-Content-SHA256`, and `X-FWallet-Signature`. System-level endpoints (`/v1/system/_*`) require the `X-System-Key` header. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { JournalLine } from './JournalLine';
import {
    JournalLineFromJSON,
    JournalLineFromJSONTyped,
    JournalLineToJSON,
    JournalLineToJSONTyped,
} from './JournalLine';

/**
 * An immutable ledger journal entry representing a financial event. Every money movement (deposit, transfer, payout, fee) is recorded as a journal entry with two or more lines that must balance. Entries are never updated or deleted — corrections are posted as new reversing entries.
 * @export
 * @interface JournalGetJournalById200Response
 */
export interface JournalGetJournalById200Response {
    /**
     * Unique journal entry identifier
     * @type {string}
     * @memberof JournalGetJournalById200Response
     */
    id: string;
    /**
     * Tenant this entry belongs to
     * @type {string}
     * @memberof JournalGetJournalById200Response
     */
    tenantId: string;
    /**
     * Client-provided key that prevents duplicate postings. If the same key is sent twice, the original entry is returned.
     * @type {string}
     * @memberof JournalGetJournalById200Response
     */
    idempotencyKey: string | null;
    /**
     * Type of financial event: deposit (MoMo/bank), transfer (wallet-to-wallet), payout_hold (funds held pending approval), payout_settlement (approved payout), payout_reversal (rejected payout hold reversal), reversal (generic correction)
     * @type {string}
     * @memberof JournalGetJournalById200Response
     */
    entryType: string;
    /**
     * Human-readable description of the entry
     * @type {string}
     * @memberof JournalGetJournalById200Response
     */
    description: string | null;
    /**
     * ISO 8601 timestamp when the entry was posted to the ledger
     * @type {string}
     * @memberof JournalGetJournalById200Response
     */
    postedAt: string;
    /**
     * ISO 8601 creation timestamp
     * @type {string}
     * @memberof JournalGetJournalById200Response
     */
    createdAt: string;
    /**
     * The individual debit/credit postings. For every entry, SUM(debit amounts) = SUM(credit amounts) — this is the double-entry invariant.
     * @type {Array<JournalLine>}
     * @memberof JournalGetJournalById200Response
     */
    lines: Array<JournalLine>;
}

/**
 * Check if a given object implements the JournalGetJournalById200Response interface.
 */
export function instanceOfJournalGetJournalById200Response(value: object): value is JournalGetJournalById200Response {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('tenantId' in value) || value['tenantId'] === undefined) return false;
    if (!('idempotencyKey' in value) || value['idempotencyKey'] === undefined) return false;
    if (!('entryType' in value) || value['entryType'] === undefined) return false;
    if (!('description' in value) || value['description'] === undefined) return false;
    if (!('postedAt' in value) || value['postedAt'] === undefined) return false;
    if (!('createdAt' in value) || value['createdAt'] === undefined) return false;
    if (!('lines' in value) || value['lines'] === undefined) return false;
    return true;
}

export function JournalGetJournalById200ResponseFromJSON(json: any): JournalGetJournalById200Response {
    return JournalGetJournalById200ResponseFromJSONTyped(json, false);
}

export function JournalGetJournalById200ResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): JournalGetJournalById200Response {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'tenantId': json['tenantId'],
        'idempotencyKey': json['idempotencyKey'],
        'entryType': json['entryType'],
        'description': json['description'],
        'postedAt': json['postedAt'],
        'createdAt': json['createdAt'],
        'lines': ((json['lines'] as Array<any>).map(JournalLineFromJSON)),
    };
}

export function JournalGetJournalById200ResponseToJSON(json: any): JournalGetJournalById200Response {
    return JournalGetJournalById200ResponseToJSONTyped(json, false);
}

export function JournalGetJournalById200ResponseToJSONTyped(value?: JournalGetJournalById200Response | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'tenantId': value['tenantId'],
        'idempotencyKey': value['idempotencyKey'],
        'entryType': value['entryType'],
        'description': value['description'],
        'postedAt': value['postedAt'],
        'createdAt': value['createdAt'],
        'lines': ((value['lines'] as Array<any>).map(JournalLineToJSON)),
    };
}

