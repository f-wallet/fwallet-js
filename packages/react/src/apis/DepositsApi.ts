/* tslint:disable */
/* eslint-disable */
/**
 * FWallet API
 * # FWallet API  Multi-tenant financial wallet infrastructure for emerging markets.  **Base URL:** `https://api.fwallet.co.ug`  ## Quick Start for Developers  1. **Create an organization** — `POST /v1/tenants` with a name, slug, and currency config 2. **Get an API key** — `POST /v1/tenants/{id}/api-keys` (save the key — it\'s shown only once) 3. **Set the header** — Add `X-API-Key: {your-key}` to all subsequent requests 4. **Create wallets** — `POST /v1/wallets` for each user/branch 5. **Fund wallets** — `POST /v1/deposits/momo-webhook` to simulate a MoMo deposit 6. **Transfer money** — `POST /v1/transfers` with an `Idempotency-Key` header 7. **Request payouts** — `POST /v1/payouts` (requires a different person to approve)  ## Key Concepts  - **Multi-tenancy**: Every API call is scoped to a tenant (organization) via the API key. Tenants are fully isolated. - **Double-entry ledger**: Every money movement is recorded as a journal entry with balanced debit/credit lines. Entries are immutable — corrections are posted as reversing entries. - **Money as bigint**: All amounts are stored in the smallest currency unit (e.g. 1 UGX = 1, 1 USD = 100 cents). API responses return these as strings. - **Idempotency**: All POST endpoints that create financial records require an `Idempotency-Key` header. Replaying the same key returns the original result. - **Maker-checker**: Payouts require approval from a person different from the requestor.  ## Error Codes  | Code | Status | Description | |------|--------|-------------| | `INSUFFICIENT_BALANCE` | 422 | Wallet doesn\'t have enough funds (amount + fee) | | `CURRENCY_MISMATCH` | 422 | Transfer between wallets of different currencies | | `WALLET_NOT_FOUND` | 404 | Wallet ID doesn\'t exist or belongs to another tenant | | `WALLET_FROZEN` | 422 | Wallet is frozen — can receive but not send | | `INVALID_API_KEY` | 401 | Missing, invalid, or expired API key | | `RISK_CHECK_FAILED` | 422 | Transaction exceeds per-transaction or daily limit | | `MAKER_CHECKER_VIOLATION` | 422 | Cannot approve your own payout request | | `UNBALANCED_ENTRY` | 422 | Journal entry debits don\'t equal credits (internal error) | | `IDEMPOTENCY_CONFLICT` | 409 | Same idempotency key with different request body |  ## Authentication  Tenant-scoped endpoints accept either bearer API keys via `X-API-Key` or HMAC signed requests via `X-FWallet-Key-Id`, `X-FWallet-Timestamp`, `X-FWallet-Nonce`, `X-FWallet-Content-SHA256`, and `X-FWallet-Signature`. System-level endpoints (`/v1/system/_*`) require the `X-System-Key` header. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  BankSlipResult,
  BankSlipVerifyRequest,
  DepositsPostDepositsBankStatements200Response,
  DepositsPostDepositsBankStatementsRequest,
  MoMoDepositResult,
  MoMoWebhookPayload,
} from '../models/index';
import {
    BankSlipResultFromJSON,
    BankSlipResultToJSON,
    BankSlipVerifyRequestFromJSON,
    BankSlipVerifyRequestToJSON,
    DepositsPostDepositsBankStatements200ResponseFromJSON,
    DepositsPostDepositsBankStatements200ResponseToJSON,
    DepositsPostDepositsBankStatementsRequestFromJSON,
    DepositsPostDepositsBankStatementsRequestToJSON,
    MoMoDepositResultFromJSON,
    MoMoDepositResultToJSON,
    MoMoWebhookPayloadFromJSON,
    MoMoWebhookPayloadToJSON,
} from '../models/index';

export interface DepositsPostDepositsBankSlipVerifyRequest {
    bankSlipVerifyRequest?: BankSlipVerifyRequest;
}

export interface DepositsPostDepositsBankStatementsOperationRequest {
    depositsPostDepositsBankStatementsRequest?: DepositsPostDepositsBankStatementsRequest;
}

export interface DepositsPostDepositsMomoWebhookRequest {
    moMoWebhookPayload?: MoMoWebhookPayload;
}

/**
 * DepositsApi - interface
 * 
 * @export
 * @interface DepositsApiInterface
 */
export interface DepositsApiInterface {
    /**
     * Creates request options for depositsPostDepositsBankSlipVerify without sending the request
     * @param {BankSlipVerifyRequest} [bankSlipVerifyRequest] 
     * @throws {RequiredError}
     * @memberof DepositsApiInterface
     */
    depositsPostDepositsBankSlipVerifyRequestOpts(requestParameters: DepositsPostDepositsBankSlipVerifyRequest): Promise<runtime.RequestOpts>;

    /**
     * Verify a bank deposit slip against imported bank statement data. A branch manager enters the slip\'s reference number and amount. The system checks if a matching bank statement line exists: if found with matching amount, the user is credited immediately (verified). If the bank feed hasn\'t arrived yet, returns pending. If the reference exists but the amount differs, returns mismatch for manual investigation.
     * @summary Create Deposits
     * @param {BankSlipVerifyRequest} [bankSlipVerifyRequest] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DepositsApiInterface
     */
    depositsPostDepositsBankSlipVerifyRaw(requestParameters: DepositsPostDepositsBankSlipVerifyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BankSlipResult>>;

    /**
     * Verify a bank deposit slip against imported bank statement data. A branch manager enters the slip\'s reference number and amount. The system checks if a matching bank statement line exists: if found with matching amount, the user is credited immediately (verified). If the bank feed hasn\'t arrived yet, returns pending. If the reference exists but the amount differs, returns mismatch for manual investigation.
     * Create Deposits
     */
    depositsPostDepositsBankSlipVerify(requestParameters: DepositsPostDepositsBankSlipVerifyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BankSlipResult>;

    /**
     * Creates request options for depositsPostDepositsBankStatements without sending the request
     * @param {DepositsPostDepositsBankStatementsRequest} [depositsPostDepositsBankStatementsRequest] 
     * @throws {RequiredError}
     * @memberof DepositsApiInterface
     */
    depositsPostDepositsBankStatementsRequestOpts(requestParameters: DepositsPostDepositsBankStatementsOperationRequest): Promise<runtime.RequestOpts>;

    /**
     * Import bank statement lines for reconciliation. These lines are used to verify bank slip deposits — when a user presents a slip, the system checks these records for a matching reference and amount. In production, this would connect to a bank feed API; for Phase 1 it accepts manual/CSV-imported data.
     * @summary Create Deposits
     * @param {DepositsPostDepositsBankStatementsRequest} [depositsPostDepositsBankStatementsRequest] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DepositsApiInterface
     */
    depositsPostDepositsBankStatementsRaw(requestParameters: DepositsPostDepositsBankStatementsOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DepositsPostDepositsBankStatements200Response>>;

    /**
     * Import bank statement lines for reconciliation. These lines are used to verify bank slip deposits — when a user presents a slip, the system checks these records for a matching reference and amount. In production, this would connect to a bank feed API; for Phase 1 it accepts manual/CSV-imported data.
     * Create Deposits
     */
    depositsPostDepositsBankStatements(requestParameters: DepositsPostDepositsBankStatementsOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DepositsPostDepositsBankStatements200Response>;

    /**
     * Creates request options for depositsPostDepositsMomoWebhook without sending the request
     * @param {MoMoWebhookPayload} [moMoWebhookPayload] 
     * @throws {RequiredError}
     * @memberof DepositsApiInterface
     */
    depositsPostDepositsMomoWebhookRequestOpts(requestParameters: DepositsPostDepositsMomoWebhookRequest): Promise<runtime.RequestOpts>;

    /**
     * Process a MoMo deposit webhook. When a user deposits via Mobile Money, the MoMo provider sends a webhook notification. FWallet verifies the signature, checks idempotency (by transactionId), and credits the user\'s wallet immediately. The deposit is recorded for later settlement reconciliation. Accounting: DR user wallet (balance increases), CR momo-clearing (settlement pending).
     * @summary Create Deposits
     * @param {MoMoWebhookPayload} [moMoWebhookPayload] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof DepositsApiInterface
     */
    depositsPostDepositsMomoWebhookRaw(requestParameters: DepositsPostDepositsMomoWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MoMoDepositResult>>;

    /**
     * Process a MoMo deposit webhook. When a user deposits via Mobile Money, the MoMo provider sends a webhook notification. FWallet verifies the signature, checks idempotency (by transactionId), and credits the user\'s wallet immediately. The deposit is recorded for later settlement reconciliation. Accounting: DR user wallet (balance increases), CR momo-clearing (settlement pending).
     * Create Deposits
     */
    depositsPostDepositsMomoWebhook(requestParameters: DepositsPostDepositsMomoWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MoMoDepositResult>;

}

/**
 * 
 */
export class DepositsApi extends runtime.BaseAPI implements DepositsApiInterface {

    /**
     * Creates request options for depositsPostDepositsBankSlipVerify without sending the request
     */
    async depositsPostDepositsBankSlipVerifyRequestOpts(requestParameters: DepositsPostDepositsBankSlipVerifyRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Nonce"] = await this.configuration.apiKey("X-FWallet-Nonce"); // hmacNonce authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-Key"] = await this.configuration.apiKey("X-API-Key"); // apiKey authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Content-SHA256"] = await this.configuration.apiKey("X-FWallet-Content-SHA256"); // hmacContentSha256 authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Timestamp"] = await this.configuration.apiKey("X-FWallet-Timestamp"); // hmacTimestamp authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Key-Id"] = await this.configuration.apiKey("X-FWallet-Key-Id"); // hmacKeyId authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Signature"] = await this.configuration.apiKey("X-FWallet-Signature"); // hmacSignature authentication
        }


        let urlPath = `/v1/deposits/bank-slip/verify`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: BankSlipVerifyRequestToJSON(requestParameters['bankSlipVerifyRequest']),
        };
    }

    /**
     * Verify a bank deposit slip against imported bank statement data. A branch manager enters the slip\'s reference number and amount. The system checks if a matching bank statement line exists: if found with matching amount, the user is credited immediately (verified). If the bank feed hasn\'t arrived yet, returns pending. If the reference exists but the amount differs, returns mismatch for manual investigation.
     * Create Deposits
     */
    async depositsPostDepositsBankSlipVerifyRaw(requestParameters: DepositsPostDepositsBankSlipVerifyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<BankSlipResult>> {
        const requestOptions = await this.depositsPostDepositsBankSlipVerifyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => BankSlipResultFromJSON(jsonValue));
    }

    /**
     * Verify a bank deposit slip against imported bank statement data. A branch manager enters the slip\'s reference number and amount. The system checks if a matching bank statement line exists: if found with matching amount, the user is credited immediately (verified). If the bank feed hasn\'t arrived yet, returns pending. If the reference exists but the amount differs, returns mismatch for manual investigation.
     * Create Deposits
     */
    async depositsPostDepositsBankSlipVerify(requestParameters: DepositsPostDepositsBankSlipVerifyRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<BankSlipResult> {
        const response = await this.depositsPostDepositsBankSlipVerifyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for depositsPostDepositsBankStatements without sending the request
     */
    async depositsPostDepositsBankStatementsRequestOpts(requestParameters: DepositsPostDepositsBankStatementsOperationRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Nonce"] = await this.configuration.apiKey("X-FWallet-Nonce"); // hmacNonce authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-Key"] = await this.configuration.apiKey("X-API-Key"); // apiKey authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Content-SHA256"] = await this.configuration.apiKey("X-FWallet-Content-SHA256"); // hmacContentSha256 authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Timestamp"] = await this.configuration.apiKey("X-FWallet-Timestamp"); // hmacTimestamp authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Key-Id"] = await this.configuration.apiKey("X-FWallet-Key-Id"); // hmacKeyId authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Signature"] = await this.configuration.apiKey("X-FWallet-Signature"); // hmacSignature authentication
        }


        let urlPath = `/v1/deposits/bank-statements`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DepositsPostDepositsBankStatementsRequestToJSON(requestParameters['depositsPostDepositsBankStatementsRequest']),
        };
    }

    /**
     * Import bank statement lines for reconciliation. These lines are used to verify bank slip deposits — when a user presents a slip, the system checks these records for a matching reference and amount. In production, this would connect to a bank feed API; for Phase 1 it accepts manual/CSV-imported data.
     * Create Deposits
     */
    async depositsPostDepositsBankStatementsRaw(requestParameters: DepositsPostDepositsBankStatementsOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DepositsPostDepositsBankStatements200Response>> {
        const requestOptions = await this.depositsPostDepositsBankStatementsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DepositsPostDepositsBankStatements200ResponseFromJSON(jsonValue));
    }

    /**
     * Import bank statement lines for reconciliation. These lines are used to verify bank slip deposits — when a user presents a slip, the system checks these records for a matching reference and amount. In production, this would connect to a bank feed API; for Phase 1 it accepts manual/CSV-imported data.
     * Create Deposits
     */
    async depositsPostDepositsBankStatements(requestParameters: DepositsPostDepositsBankStatementsOperationRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DepositsPostDepositsBankStatements200Response> {
        const response = await this.depositsPostDepositsBankStatementsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for depositsPostDepositsMomoWebhook without sending the request
     */
    async depositsPostDepositsMomoWebhookRequestOpts(requestParameters: DepositsPostDepositsMomoWebhookRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Nonce"] = await this.configuration.apiKey("X-FWallet-Nonce"); // hmacNonce authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-Key"] = await this.configuration.apiKey("X-API-Key"); // apiKey authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Content-SHA256"] = await this.configuration.apiKey("X-FWallet-Content-SHA256"); // hmacContentSha256 authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Timestamp"] = await this.configuration.apiKey("X-FWallet-Timestamp"); // hmacTimestamp authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Key-Id"] = await this.configuration.apiKey("X-FWallet-Key-Id"); // hmacKeyId authentication
        }

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-FWallet-Signature"] = await this.configuration.apiKey("X-FWallet-Signature"); // hmacSignature authentication
        }


        let urlPath = `/v1/deposits/momo-webhook`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MoMoWebhookPayloadToJSON(requestParameters['moMoWebhookPayload']),
        };
    }

    /**
     * Process a MoMo deposit webhook. When a user deposits via Mobile Money, the MoMo provider sends a webhook notification. FWallet verifies the signature, checks idempotency (by transactionId), and credits the user\'s wallet immediately. The deposit is recorded for later settlement reconciliation. Accounting: DR user wallet (balance increases), CR momo-clearing (settlement pending).
     * Create Deposits
     */
    async depositsPostDepositsMomoWebhookRaw(requestParameters: DepositsPostDepositsMomoWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<MoMoDepositResult>> {
        const requestOptions = await this.depositsPostDepositsMomoWebhookRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => MoMoDepositResultFromJSON(jsonValue));
    }

    /**
     * Process a MoMo deposit webhook. When a user deposits via Mobile Money, the MoMo provider sends a webhook notification. FWallet verifies the signature, checks idempotency (by transactionId), and credits the user\'s wallet immediately. The deposit is recorded for later settlement reconciliation. Accounting: DR user wallet (balance increases), CR momo-clearing (settlement pending).
     * Create Deposits
     */
    async depositsPostDepositsMomoWebhook(requestParameters: DepositsPostDepositsMomoWebhookRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<MoMoDepositResult> {
        const response = await this.depositsPostDepositsMomoWebhookRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
